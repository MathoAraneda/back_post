export class Salida {
    constructor(codeNumber, message, data, error)
    {
        this.codeNumber = codeNumber;
        this.message = message; 
        this.data = data;
        this.error = error;
    }
}

export const codes = () => {
    
}